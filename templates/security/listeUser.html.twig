{% extends 'security/security.html.twig' %}

{% block container %}
    <ul>
        {% for user in users %}
            <li><a href="{{ path('showUser', {'id': user.id}) }}">{{ user.email }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
