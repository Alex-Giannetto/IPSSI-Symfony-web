{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <ul>
            {% for video in videos %}
                <li>{{ video.title }}</li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}